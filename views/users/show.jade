extends ../layout

block content
	h1.
		Infophoto #{user._id}
	p Name: #{user.name}
	p Email: #{user.email}
	p Is Admin: #{user.isAdmin}

	p
		a(href='/users') Back
		|    
		a(href='/users/#{user._id}/edit') Update
	